spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        cache:
          use_second_level_cache: false

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.springframework.web: DEBUG
    com.github.k1mb1.vkr_backend: DEBUG
