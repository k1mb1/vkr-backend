/**
 * Контингент: группы, подгруппы и студенты — один агрегат ростера (студент всегда
 * принадлежит группе, ростер управляется через эндпоинты групп). Связью
 * «предмет—группы» владеет subject и реализует порт {@code group.api.GroupSubjectsPort}.
 */
package com.github.k1mb1.vkr_backend.group;
