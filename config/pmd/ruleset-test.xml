<?xml version="1.0"?>
<ruleset name="Test rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        PMD rules for test sources. Same intent as the main rule set, but the
        unit-test "style" rules are relaxed: ArchUnit tests assert via
        .check(), Spring test helpers are named like tests without
        containing cases, and multiple asserts per test are fine here.
    </description>

    <rule ref="category/java/errorprone.xml">
        <exclude name="AvoidLiteralsInIfCondition"/>
        <exclude name="AvoidDuplicateLiterals"/>
        <exclude name="MissingSerialVersionUID"/>
        <exclude name="AvoidFieldNameMatchingMethodName"/>
        <exclude name="ConstructorCallsOverridableMethod"/>
        <exclude name="NullAssignment"/>
        <!-- Spring test helpers (TestApplication, TestcontainersConfiguration). -->
        <exclude name="TestClassWithoutTestCases"/>
    </rule>

    <rule ref="category/java/bestpractices.xml">
        <exclude name="GuardLogStatement"/>
        <exclude name="AbstractClassWithoutAbstractMethod"/>
        <exclude name="UnitTestShouldIncludeAssert"/>
        <exclude name="UnitTestContainsTooManyAsserts"/>
        <exclude name="UnitTestAssertionsShouldIncludeMessage"/>
        <!-- ArchUnit's JavaClasses type trips this; not meaningful in tests. -->
        <exclude name="LooseCoupling"/>
    </rule>

    <rule ref="category/java/performance.xml"/>

    <rule ref="category/java/design.xml">
        <exclude name="LawOfDemeter"/>
        <exclude name="LoosePackageCoupling"/>
        <exclude name="ExcessiveImports"/>
        <exclude name="TooManyMethods"/>
        <exclude name="DataClass"/>
        <!-- ArchUnit @AnalyzeClasses tests hold only static @ArchTest rule fields,
             which trips this; they are test classes, not utility classes. -->
        <exclude name="UseUtilityClass"/>
        <!-- ArchConditions is an intentional collection of small, independent
             reusable ArchCondition factories (kept in one place by design), which
             trips this size metric. -->
        <exclude name="GodClass"/>
    </rule>
</ruleset>
